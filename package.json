{
  "type": "module",
  "scripts": {
    "generate-config": "node scripts/generate-public-config.mjs",
    "predev": "npm run generate-config",
    "dev": "vite",
    "prebuild": "npm run generate-config",
    "build": "vite build",
    "prepreview": "npm run generate-config",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "autoprefixer": "^10.4.0",
    "lucide-react": "^0.468.0",
    "postcss": "^8.4.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
